# Server configuration
server.port=${PORT:8080}
server.error.include-message=always

# CORS configuration (allows your frontend origins)
spring.mvc.cors.allowed-origins=http://localhost:5500,http://localhost:5501,https://techy-ui.github.io,http://localhost:3000
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*
spring.mvc.cors.allow-credentials=true

spring.datasource.url=jdbc:postgresql://db.eikmelxlgrejsvknmnvu.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Techyui@1612
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging configuration
logging.level.com.example.codecare=DEBUG
# Log all web requests (e.g., see incoming POST) - set to DEBUG for detailed request tracing
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.boot=INFO

# Optional: Expose health endpoint for better monitoring
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always